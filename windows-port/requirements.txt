# Runtime
pywebview>=5.0
yt-dlp>=2025.1.0
faster-whisper>=1.0.0
openai>=1.0.0
httpx>=0.27.0

# Build (icon generation)
Pillow>=10.0.0

# Windows-specific dependencies
# pywebview has different backends for Windows:
# - For WebView2 (Windows 10+): No additional dependencies needed
# - For older Windows: Install WebKit2 via the installer
