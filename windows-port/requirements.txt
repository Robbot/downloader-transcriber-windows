# Runtime
pywebview>=5.0
yt-dlp>=2025.1.0
faster-whisper>=1.0.0
openai>=1.0.0
httpx>=0.27.0

# Build (icon generation)
Pillow>=10.0.0

# Windows-specific dependencies
# pythonnet must be installed BEFORE pywebview to avoid build errors
pythonnet>=3.0.0
# Install with: pip install --prefer-binary pythonnet
